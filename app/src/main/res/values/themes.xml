<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CarryUs" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:includeFontPadding">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:statusBarColor">@color/white000</item>
        <item name="colorPrimary">@color/orange400</item>
    </style>

    <style name="Theme.CarryUs" parent="Base.Theme.CarryUs" />

    <style name="BottomNavigationViewNoRipple" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:itemBackground">@android:color/transparent</item>
        <item name="android:colorControlHighlight">@android:color/transparent</item>
    </style>

    <style name="CustomRatingBar" parent="@android:style/Widget.Material.Light.RatingBar">
        <item name="android:progressDrawable">@drawable/bg_ratingbar</item>
    </style>
</resources>
